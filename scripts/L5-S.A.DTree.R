# Decision Tree is a classification or supervised algorithm
# The dataset use is the titanic data set from 
str(Titanic)
class(Titanic) # the bult in dataset is in table format
# convert to a dataframe
shipdata<-as.data.frame(Titanic)
class(shipdata)
str(shipdata)
